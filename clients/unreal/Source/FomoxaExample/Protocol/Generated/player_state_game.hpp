// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: Models/protocol.hpp
// model: PlayerState
// codec: game
// fingerprint: sha256:841f5f0775c65445c2d9c9c6a532ee11522104c9f7d1d9195384d1488129dacd
// fomoxac-version: 0.2.0
// generated-at: 2026-09-15T10:26:50Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "Models/protocol.hpp"

namespace generated {

/// The "game" codec for PlayerState, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `PlayerId`: `u32`
///  1. `ClientKind`: `u8`
///  2. `Color`: `u32`
///  3. `PositionX`: `f32`
///  4. `PositionZ`: `f32`
///  5. `PositionY`: `f32`
///  6. `LookYaw`: `f32`
///  7. `LookPitch`: `f32`
struct PlayerStateGameCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "PlayerState.game";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x9C787B03u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x841F5F0775C65445ULL;

    /// Writes the "game" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::FomoxaExampleModels::PlayerState& value) {
        writer.write_u32(value.PlayerId);
        writer.write_u8(value.ClientKind);
        writer.write_u32(value.Color);
        writer.write_f32(value.PositionX);
        writer.write_f32(value.PositionZ);
        writer.write_f32(value.PositionY);
        writer.write_f32(value.LookYaw);
        writer.write_f32(value.LookPitch);
    }

    /// Reads the "game" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::FomoxaExampleModels::PlayerState& value) {
        if (reader.field_absent()) {
            value.PlayerId = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.PlayerId); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.ClientKind = 0;
        } else {
            if (DecodeError error = reader.read_u8(value.ClientKind); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.Color = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Color); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.PositionX = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.PositionX); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.PositionZ = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.PositionZ); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.PositionY = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.PositionY); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.LookYaw = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.LookYaw); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.LookPitch = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.LookPitch); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
