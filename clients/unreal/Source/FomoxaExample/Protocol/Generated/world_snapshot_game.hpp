// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: Models/protocol.hpp
// model: WorldSnapshot
// codec: game
// fingerprint: sha256:3966ddba99fe7647b3b8bcac62ece223b4143800040a73d0854fc3c7f3788724
// fomoxac-version: 0.2.0
// generated-at: 2026-09-15T10:26:50Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "Models/protocol.hpp"
#include "player_state_game.hpp"

namespace generated {

/// The "game" codec for WorldSnapshot, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `Tick`: `u32`
///  1. `Players`: `Array<PlayerState>`
struct WorldSnapshotGameCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "WorldSnapshot.game";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x7DDE7B54u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x3966DDBA99FE7647ULL;

    /// Writes the "game" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::FomoxaExampleModels::WorldSnapshot& value) {
        writer.write_u32(value.Tick);
        writer.write_array_count(value.Players.size());
        for (const auto& element : value.Players) {
            PlayerStateGameCodec::encode(writer, element);
        }
    }

    /// Reads the "game" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::FomoxaExampleModels::WorldSnapshot& value) {
        if (reader.field_absent()) {
            value.Tick = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Tick); !error.ok()) return error;
        }
        {
            std::size_t count = 0;
            if (!reader.field_absent()) {
                if (DecodeError error = reader.read_array_count(count); !error.ok()) return error;
            }
            std::vector<::FomoxaExampleModels::PlayerState> elements;
            elements.reserve(count);
            for (std::size_t i = 0; i < count; ++i) {
                ::FomoxaExampleModels::PlayerState element{};
                if (DecodeError error = PlayerStateGameCodec::decode(reader, element); !error.ok()) return error;
                elements.push_back(std::move(element));
            }
            value.Players = std::move(elements);
        }
        return DecodeError{};
    }
};

}  // namespace generated
