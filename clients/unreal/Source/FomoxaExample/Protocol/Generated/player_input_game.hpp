// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: Models/protocol.hpp
// model: PlayerInput
// codec: game
// fingerprint: sha256:b12f80f42bd84e528439e71ad050e1ec6736bc1bf38bf6d7cd1e3948d1367439
// fomoxac-version: 0.2.0
// generated-at: 2026-09-15T10:26:50Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "Models/protocol.hpp"

namespace generated {

/// The "game" codec for PlayerInput, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `Sequence`: `u32`
///  1. `MoveX`: `f32`
///  2. `MoveZ`: `f32`
///  3. `Jump`: `bool`
///  4. `LookYaw`: `f32`
///  5. `LookPitch`: `f32`
struct PlayerInputGameCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "PlayerInput.game";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x6984A5A4u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0xB12F80F42BD84E52ULL;

    /// Writes the "game" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::FomoxaExampleModels::PlayerInput& value) {
        writer.write_u32(value.Sequence);
        writer.write_f32(value.MoveX);
        writer.write_f32(value.MoveZ);
        writer.write_bool(value.Jump);
        writer.write_f32(value.LookYaw);
        writer.write_f32(value.LookPitch);
    }

    /// Reads the "game" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::FomoxaExampleModels::PlayerInput& value) {
        if (reader.field_absent()) {
            value.Sequence = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Sequence); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.MoveX = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.MoveX); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.MoveZ = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.MoveZ); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.Jump = false;
        } else {
            if (DecodeError error = reader.read_bool(value.Jump); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.LookYaw = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.LookYaw); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.LookPitch = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.LookPitch); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
