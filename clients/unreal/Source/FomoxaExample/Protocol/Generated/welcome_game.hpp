// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: Models/protocol.hpp
// model: Welcome
// codec: game
// fingerprint: sha256:2d2df98b821dcd9d354a5eedca3353043dd13d64615e70b325e0789c1c1fd4ee
// fomoxac-version: 0.2.0
// generated-at: 2026-09-15T10:26:50Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "Models/protocol.hpp"

namespace generated {

/// The "game" codec for Welcome, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `PlayerId`: `u32`
///  1. `PlaneHalfSize`: `f32`
///  2. `TickRate`: `u16`
struct WelcomeGameCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "Welcome.game";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x00876FB0u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x2D2DF98B821DCD9DULL;

    /// Writes the "game" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::FomoxaExampleModels::Welcome& value) {
        writer.write_u32(value.PlayerId);
        writer.write_f32(value.PlaneHalfSize);
        writer.write_u16(value.TickRate);
    }

    /// Reads the "game" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::FomoxaExampleModels::Welcome& value) {
        if (reader.field_absent()) {
            value.PlayerId = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.PlayerId); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.PlaneHalfSize = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.PlaneHalfSize); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.TickRate = 0;
        } else {
            if (DecodeError error = reader.read_u16(value.TickRate); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
