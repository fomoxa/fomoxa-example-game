// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: Models/protocol.hpp
// model: ClientHello
// codec: game
// fingerprint: sha256:1d75366230b9d0ef68ee449a36f64e88439183e2eedff1da0cf7ebd551f44f1e
// fomoxac-version: 0.2.0
// generated-at: 2026-09-15T10:26:50Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "Models/protocol.hpp"

namespace generated {

/// The "game" codec for ClientHello, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `ClientKind`: `u8`
///  1. `DisplayName`: `string`
struct ClientHelloGameCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "ClientHello.game";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0xACEB4840u;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x1D75366230B9D0EFULL;

    /// Writes the "game" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::FomoxaExampleModels::ClientHello& value) {
        writer.write_u8(value.ClientKind);
        writer.write_string(value.DisplayName);
    }

    /// Reads the "game" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::FomoxaExampleModels::ClientHello& value) {
        if (reader.field_absent()) {
            value.ClientKind = 0;
        } else {
            if (DecodeError error = reader.read_u8(value.ClientKind); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.DisplayName = std::string();
        } else {
            if (DecodeError error = reader.read_string(value.DisplayName); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
