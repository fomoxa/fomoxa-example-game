# GENERATED BY fomoxac
# DO NOT EDIT MANUALLY
# source: network/models/world_snapshot.gd
# model: WorldSnapshot
# codec: game
# fingerprint: sha256:3966ddba99fe7647b3b8bcac62ece223b4143800040a73d0854fc3c7f3788724
# fomoxac-version: 0.2.0
# generated-at: 2026-09-15T09:11:53Z

class_name WorldSnapshotGameCodec

# WorldSnapshotGameCodec is the "game" codec for WorldSnapshot, generated from its Fomoxa directives.
#
# The wire layout, in declaration order (RFC-0002 §5.1):
#
#  0. `tick`: `u32`
#  1. `players`: `Array<PlayerState>`

# This message's name: Model.codec.
const MESSAGE_NAME := "WorldSnapshot.game"

# This message's stable id, derived from its name alone.
const MESSAGE_ID: int = 0x7DDE7B54

# This message's wire-contract fingerprint - the same value handshake.gd publishes,
# and the one a peer compares against.
const FINGERPRINT: int = (0x3966DDBA << 32) | 0x99FE7647

# Writes the "game" fields of value, in declaration order.
static func encode(writer: FomoxaRuntime.Writer, value: WorldSnapshot) -> void:
	writer.write_u32(value.tick)
	writer.write_array_count(value.players.size())
	for players_element in value.players:
		PlayerStateGameCodec.encode(writer, players_element)

# Reads the "game" fields into value, in declaration order.
#
# Fields this codec does not carry are left as they were, which is what lets one
# model be split across several codecs.
#
# A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
# the stream ended inside is an error. Bytes left over after the last field
# belong to a newer writer's model and are ignored.
static func decode(reader: FomoxaRuntime.Reader, value: WorldSnapshot) -> FomoxaRuntime.DecodeError:
	if reader.field_absent():
		value.tick = 0
	else:
		var tick_result = reader.read_u32()
		if tick_result[1] != null:
			return tick_result[1]
		value.tick = tick_result[0]
	var players_count := 0
	if not reader.field_absent():
		var players_count_result := reader.read_array_count()
		if players_count_result[1] != null:
			return players_count_result[1]
		players_count = players_count_result[0]
	var players_elements := []
	for players_i in range(players_count):
		var players_element = PlayerState.new()
		var players_element_error = PlayerStateGameCodec.decode(reader, players_element)
		if players_element_error != null:
			return players_element_error
		players_elements.append(players_element)
	value.players = players_elements
	return null
