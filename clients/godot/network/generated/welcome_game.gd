# GENERATED BY fomoxac
# DO NOT EDIT MANUALLY
# source: network/models/welcome.gd
# model: Welcome
# codec: game
# fingerprint: sha256:2d2df98b821dcd9d354a5eedca3353043dd13d64615e70b325e0789c1c1fd4ee
# fomoxac-version: 0.2.0
# generated-at: 2026-09-15T09:00:33Z

class_name WelcomeGameCodec

# WelcomeGameCodec is the "game" codec for Welcome, generated from its Fomoxa directives.
#
# The wire layout, in declaration order (RFC-0002 §5.1):
#
#  0. `player_id`: `u32`
#  1. `plane_half_size`: `f32`
#  2. `tick_rate`: `u16`

# This message's name: Model.codec.
const MESSAGE_NAME := "Welcome.game"

# This message's stable id, derived from its name alone.
const MESSAGE_ID: int = 0x00876FB0

# This message's wire-contract fingerprint - the same value handshake.gd publishes,
# and the one a peer compares against.
const FINGERPRINT: int = (0x2D2DF98B << 32) | 0x821DCD9D

# Writes the "game" fields of value, in declaration order.
static func encode(writer: FomoxaRuntime.Writer, value: Welcome) -> void:
	writer.write_u32(value.player_id)
	writer.write_f32(value.plane_half_size)
	writer.write_u16(value.tick_rate)

# Reads the "game" fields into value, in declaration order.
#
# Fields this codec does not carry are left as they were, which is what lets one
# model be split across several codecs.
#
# A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
# the stream ended inside is an error. Bytes left over after the last field
# belong to a newer writer's model and are ignored.
static func decode(reader: FomoxaRuntime.Reader, value: Welcome) -> FomoxaRuntime.DecodeError:
	if reader.field_absent():
		value.player_id = 0
	else:
		var player_id_result = reader.read_u32()
		if player_id_result[1] != null:
			return player_id_result[1]
		value.player_id = player_id_result[0]
	if reader.field_absent():
		value.plane_half_size = 0.0
	else:
		var plane_half_size_result = reader.read_f32()
		if plane_half_size_result[1] != null:
			return plane_half_size_result[1]
		value.plane_half_size = plane_half_size_result[0]
	if reader.field_absent():
		value.tick_rate = 0
	else:
		var tick_rate_result = reader.read_u16()
		if tick_rate_result[1] != null:
			return tick_rate_result[1]
		value.tick_rate = tick_rate_result[0]
	return null
